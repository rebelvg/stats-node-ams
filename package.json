{
  "name": "stats-node-ams",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "docker": "docker-compose up -d && yarn remove-app && yarn install-app",
    "remove-app": "docker exec ams rm -rf /opt/adobe/ams/applications/live",
    "install-app": "docker cp ams_app/live ams:/opt/adobe/ams/applications"
  }
}
